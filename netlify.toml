[build]
  command = "mkdir -p dist && pnpm install && pnpm run build"
  publish = "dist"
  functions = "functions"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/redirect.php"
  to = "/.netlify/functions/redirect"
  status = 301
  force = true


  